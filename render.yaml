services:
  - type: web
    name: shopify-infakt
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: INFAKT_API_TOKEN
        fromService: INFAKT_API_TOKEN